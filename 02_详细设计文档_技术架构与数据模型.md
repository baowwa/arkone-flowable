# 详细设计文档

## 技术架构

### 系统架构
- 描述系统组件和服务之间的关系。
- 可以使用图示来展示各模块的交互。

### 数据库 DDL
- 数据库表结构定义
- 可以提供 SQL 脚本示例，如下所示：
    ```sql
    CREATE TABLE users (
        id INT PRIMARY KEY,
        name VARCHAR(100),
        created_at TIMESTAMP
    );
    ```  
- 列出各表的字段和数据类型。

## 实体关系
- 描述各实体之间的关系，如一对一、一对多或多对多关系。
- 可以提供 ER 图的示例。

## 附录
- 包含其他相关信息，如 API 设计、技术栈选择等。